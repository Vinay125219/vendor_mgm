---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div
  class:list={[
    'animate-pulse rounded-[var(--radius-vs-sm)] bg-vs-border/50 dark:bg-vs-border/30',
    className,
  ]}
  aria-hidden="true"
/>
